{
  "name": "mdkg",
  "version": "0.0.1",
  "description": "Markdown Knowledge Graph",
  "license": "MIT",
  "bin": {
    "mdkg": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/add-shebang.js",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test dist/tests/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "typescript": "^5.4.0"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/nickreames/mdkg.git"
  },
  "keywords": [
    "markdown",
    "knowledge-graph",
    "cli"
  ]
}
