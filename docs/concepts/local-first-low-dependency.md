# Local-first and Low-dependency

Markdown and Git remain the durable project memory layer. Generated indexes are rebuildable, and optional project DB state stays local.

See the Starlight source at `docs/src/content/docs/concepts/local-first-low-dependency.md`.
